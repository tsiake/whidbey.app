# whidbey.app client side
******
```
client side configuration, stores all code for what the user sees.

/api contains client-side api calls
/assets contains anything contained in css / imgs
/components contains react components which are accessed by App.jsx
/redux is for redux store

to do: need to fix apis / clean up code - done
```
